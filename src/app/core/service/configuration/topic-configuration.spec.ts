import { TopicConfiguration } from './topic-configuration';

describe('TopicConfiguration', () => {
  it('should create an instance', () => {
    expect(new TopicConfiguration()).toBeTruthy();
  });
});
